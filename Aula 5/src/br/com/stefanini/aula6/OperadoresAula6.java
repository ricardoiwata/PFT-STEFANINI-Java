package br.com.stefanini.aula6;

public class OperadoresAula6 {
	public static void main(String[] args) {
		int a = 10; // operador de atriuicao
		int b = 5; 
		
		b+=a; //aditivo
		b= b+a;
		b -= a; // subrtrativo
		b= b-a;
		b *= a; // multiplicativo
		b= b*a;
		b/=a; //divisor
		b= b/a;
		b %= a; // modular
		
		a++; 
		
		// == comparação
		// != diferente
		// >
		// < 
		// <=
		// >=
		
		//AND = &&
		//OR = ||
		//XOR = !=
		// NEGAÇÃO = !
		
		// Ternário = ? 
		// comparação ? se for verdadeiro : se re
		

	}

}
